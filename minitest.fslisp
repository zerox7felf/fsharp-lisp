(->
    (let pang cond (->
        (let pong (
            print HejsanFromPong
        ))
        // (if (cond()) (
        //     pang
        // )(
        //     hej
        // ))
        (pong)
    ))

    (let hej (->
        (12)
    ))

    // ((pang false)())
    (pang false)

    // ((pang false)())

    (hej())
)
