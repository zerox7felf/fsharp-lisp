(->
    (define pang cond (->
        (define pong (
            print HejsanFromPong
        ))
        // (if (cond()) (
        //     pang
        // )(
        //     hej
        // ))
        (pong)
    ))

    (define hej (->
        (12)
    ))

    // ((pang false)())
    (pang false)

    // ((pang false)())

    (hej())
)
